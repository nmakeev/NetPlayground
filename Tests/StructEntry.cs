namespace Playground;

public struct StructEntry
{
    public readonly int Id;
    public readonly float Weight;

    public StructEntry(int id, float weight)
    {
        Id = id;
        Weight = weight;
    }
}